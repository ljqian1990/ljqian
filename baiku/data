附录：程序代码#include<string.h>#include<iostream.h> #include <windows.h> #define N 10000 int FLAG=1;class std{int No;char name[10];float Eng;float Chi;float Mat;float Phy;float Geo;float total; public: void setname(char na[])  {strcpy(name,na);  };void setNo(int n){No=n;};void setEng(float e) {Eng=e;};void setChi(float c){Chi=c;};void setMat(float m){Mat=m;};void setPhy(float p){Phy=p;};void setGeo(float g){Geo=g;};void settotal(float t){total=t;};char* getname(){return name;};int getNo(){return No;};float getEng(){return Eng;  };float getChi(){return Chi;  };float getMat(){return Mat;  };float getPhy(){return Phy;  };float getGeo(){return Geo;  };float gettotal(){return total;  };}; class compute{int ns; int addns; std na[N]; static float sumEng; static float sumChi; static float sumMat; static float sumPhy; static float sumGeo;public: compute() {  } ns=0; void getdata(); //输入数据 void sort(); //按照成绩总分进行排序  void disp();//显示结果 void initdisp();//初始化界面 int usechoice();//根据用户选择获取不同的操作 void adddata();//添加学生记录 void namefind(int); //按照姓名查找记录  void dispone(int i);//显示一个学生的记录 int inputonename();  //输入一个学生的姓名  void dele(int num); //删除学生记录 void showaver(int num); void quit();//退出系统 //显示学生平均分float aveEng() {  }  return sumEng/ns;float aveChi(){return sumChi/ns;  }float aveMat(){return sumMat/ns;  }float avePhy(){return sumPhy/ns;  }float aveChem(){return sumGeo/ns;  }int getns(){return ns;}}; float compute::sumEng=0.0; float compute::sumChi=0.0; float compute::sumMat=0.0; float compute::sumPhy=0.0; float compute::sumGeo=0.0;void compute::getdata() { float En,Ch,Ma,Ph,Ge,Tol; char tname[10];cout<<"Please input the amounts of students!"; cin>>ns; for(i=0;i<ns;i++) {                        cout<<"Input the student's NO:"; cin>>sno; na[i].setNo(sno); cout<<"Input the student's name:"; cin>>tname; na[i].setname(tname); cout<<"Input the student's English score:"; cin>>En; sumEng=sumEng+En; na[i].setEng(En); cout<<"Input the student's Chinese score:"; cin>>Ch; sumChi=sumChi+Ch; na[i].setChi(Ch); cout<<"Input the student's Maths score:"; cin>>Ma; sumMat=sumMat+Ma; na[i].setMat(Ma); cout<<"Input the student's Physics score:"; cin>>Ph; sumPhy=sumPhy+Ph; na[i].setPhy(Ph); cout<<"Input the student's Geography score:"; }     } sumGeo=sumGeo+Ge; na[i].setGeo(Ge); Tol=En+Ch+Ma+Ph+Ge; na[i].settotal(Tol);void compute::sort()  //直接选择排序{ } void compute::disp(){cout<<"List"<<"  NO"<<"  Name"<<"  English"<<"  Chinese"<<"  Maths"<<"  int i,j,pick; std temp; for(i=0;i<ns-1;i++) {          } pick=i; for(j=i+1;j<ns;j++) {   } temp=na[i]; na[i]=na[pick]; na[pick]=temp; if(na[j].gettotal()>na[pick].gettotal())  pick=j; Physics"<<"  Geography"<<"  Total "<<endl; for(int i=0;i<ns;i++)cout<<i+1<<"     "<<na[i].getNo()<<"    "<<na[i].getname()<<"      "<<na[i].getEng()<<"       "<<na[i].getChi()<<"      "<<na[i].getMat()<<"    "<<na[i].getPhy()<<"    "<<na[i].getGeo()<<"    "<<na[i].gettotal()<<endl; } void compute::initdisp(){ } int compute::usechoice(){ int opt; cout<<endl;} cout<<"**********************************************"<<endl; cout<<"             学生成绩管理系统                             "<<endl; cout<<"**********************************************"<<endl; cout<<endl; cout<<"请根据要求选择下面的操作:"<<endl; cout<<"1、输入学生成绩记录"<<endl; cout<<"2、添加学生记录"<<endl; cout<<"3、按照学号查找学生记录"<<endl; cout<<"4、删除学生记录"<<endl; cout<<"5、按照总分排序"<<endl; cout<<"6、显示学生平均分"<<endl; cout<<"7、显示学生成绩记录信息"<<endl; cout<<"8、退出系统"<<endl; }cout<<"请选择相应的操作，输入序号即可:"; cin>>opt; return opt;void compute::adddata(){ int i,sno; float En,Ch,Ma,Ph,Ge,Tol; char tname[10]; if(ns==0) {   } cout<<"请输入要添加的学生记录"; cin>>addns; for(i=0;i<addns;i++) {            cout<<"Input the student's NO:"; cin>>sno; na[ns+i].setNo(sno); cout<<"Input the student's name:"; cin>>tname; na[ns+i].setname(tname); cout<<"Input the student's English score:"; cin>>En; sumEng=sumEng+En; na[ns+i].setEng(En); cout<<"Input the student's Chinese score:"; cout<<"请先选择1添加数据，然后在选择2增加记录"<<endl; return ; }} cin>>Ch; sumChi=sumChi+Ch; na[ns+i].setChi(Ch); cout<<"Input the student's Maths score:"; cin>>Ma; sumMat=sumMat+Ma; na[ns+i].setMat(Ma); cout<<"Input the student's Physics score:"; cin>>Ph; sumPhy=sumPhy+Ph; na[ns+i].setPhy(Ph); cout<<"Input the student's Geography score:"; cin>>Ge; sumGeo=sumGeo+Ge; na[ns+i].setGeo(Ge); Tol=En+Ch+Ma+Ph+Ge; na[ns+i].settotal(Tol); ns=ns+addns;void compute::dispone(int i){cout<<"List"<<"  NO"<<"  Name"<<"  English"<<"  Chinese"<<"  Maths"<<"  Total "<<endl;cout<<1<<"     "<<na[i].getNo()<<"    "<<na[i].getname()<<"      "<<na[i].getEng()<<"       "<<na[i].getChi()<<"      "<<na[i].getMat()<<"    "<<na[i].getPhy()<<"    "<<na[i].getGeo()<<"    "<<na[i].gettotal()<<endl;}void compute::namefind(int num){int i;for(i=0;i<ns;i++) } int compute::inputonename(){ } void compute::dele(int num){ {     } if(i>=ns) {  } cout<<"没有您要查的记录"<<endl; if(na[i].getNo()==num) {  } dispone(i); int num; cout<<"请输入要查找的学生学号:"; cin>>num; return num; int i,cunt,flag=0; //学号是否存在 if(num<0) {  } cout<<"超出学号范围"<<endl; return ; for(i=0;i<ns;i++) {     if(na[i].getNo()==num) {   //记录该学号在数组中的位置 cunt = i;   }  } flag=1; if(!flag) {   } cout<<"学号不存在"<<endl; return ;for(i=cunt;i<ns-1;i++) {         } ns = ns - 1;  na[i].setname(na[i+1].getname()); na[i].setNo(na[i+1].getNo()); na[i].setEng(na[i+1].getEng()); na[i].setChi(na[i+1].getChi()); na[i].setMat(na[i+1].getMat()); na[i].setPhy(na[i+1].getPhy()); na[i].setGeo(na[i+1].getGeo()); na[i].settotal(na[i+1].gettotal()); void compute::showaver(int num){ } void compute::quit(){ }void main(){ int i; for(i=0;i<ns;i++) {      } if(na[i].getNo()==num) {   } cout<<"学号:"<<na[i].getNo()<<endl; cout<<"姓名:"<<na[i].getname()<<endl; FLAG=0; cout<<"程序在3秒后关闭……"<<endl; _sleep(1000); cout<<"还剩2秒……"<<endl; _sleep(1000); cout<<"还剩1秒……"<<endl; _sleep(1000); return ; compute obj; int opt;  ndl; ndl; {    obj.initdisp();    opt=obj.usechoice();    switch(opt)    {       case 1:  cout<<"=============================================="<<e      obj.getdata(); cout<<"=============================================="<<e     break;       case 2:cout<<"=============================================="<<endl; obj.adddata();cout<<"=============================================="<<endl; break;   case 3:cout<<"=============================================="<<endl; int No;  No = obj.inputonename();  obj.namefind(No);cout<<"=============================================="<<endl; case 4:cout<<"=============================================="<<endl; int deno;   deno = obj.inputonename();   obj.dele(deno);   obj.disp();cout<<"=============================================="<<endl; break;   case 5:cout<<"=============================================="<<endl; obj.sort();   obj.disp();cout<<"=============================================="<<endl; break;   case 6:cout<<"=============================================="<<endl; cout<<"英语平均分:"<<obj.aveEng()<<endl;   cout<<"语文平均分:"<<obj.aveChi()<<endl;           cout<<"数学平均分:"<<obj.aveMat()<<endl;         cout<<"物理平均分:"<<obj.aveMat()<<endl;   cout<<"地理平均分:"<<obj.aveMat()<<endl;cout<<"=============================================="<<endl; break;  case 7:cout<<"=============================================="<<endl; obj.disp();cout<<"=============================================="<<endl; break;   case 8:cout<<"=============================================="<<endl; obj.quit();cout<<"=============================================="<<endl; break;       default:cout<<"=============================================="<<endl; cout<<"您选择的记录是错误的，谢谢;"<<endl;cout<<"=============================================="<<endl; } } }